# Project: [service name]

Stack:    [Python/Django | Node/TS | other]
DB:       [postgres | mongo] → [schema name]
Queue:    [celery | bullmq | none]
Tests:    uv run pytest | yarn test
Lint:     uv run ruff check . | yarn lint

# Domain map
# [domain] → [what it owns]

# Out of scope for this service
# List things Claude should NOT touch or add here

# Known gotchas
# Anything non-obvious about this codebase
